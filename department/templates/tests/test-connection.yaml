apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "department.fullname" . }}-test-connection"
  labels:
{{ include "department.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "department.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
