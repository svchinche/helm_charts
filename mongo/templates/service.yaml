apiVersion: v1
kind: Service
metadata:
   name: db
   labels:
      app: mongodb
spec:
   ports:
   -  port: {{ DB.MONGO.PORT }}
      protocol: TCP
   selector:
      role: {{ .Values.role }}
      environment: {{ .Values.profile }}
   clusterIP: None
